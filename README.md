# Pokeapi-Spring-React-Docker-AWS

This Application use docker to generate two container services, backend and frontend.

## 🚀&nbsp; To start the Application
1. docker-compose up --build  # -d to run in background

